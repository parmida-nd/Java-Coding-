/**
 * Enum OrderStatus represents the various statuses that an order can have.
 * Each status indicates a different stage in the order lifecycle.
 */
public enum OrderStatus 
{
    PENDING,
    COMPLETED,
    REFUNDED,
    CANCELLED
}